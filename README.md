# Telegram-bot-for-reposts
Simple Telegram bot that forwards messages from source channels (or groups) to target channels based on precise keyword matching.    You manage the bot entirely through its own Telegram chat interface. It allows you to create workspaces, link source channels, add keywords, and set target channels without ever needing to touch a database.
